{
  "enabled": true,
  "name": "Service Test Generator",
  "description": "Automatically creates corresponding test files in __tests__/services/ when new files are added to the services/ directory",
  "version": "1",
  "when": {
    "type": "fileEdited",
    "patterns": [
      "backend/src/services/**/*.ts"
    ]
  },
  "then": {
    "type": "askAgent",
    "prompt": "A new service file has been created at {filePath}. Please generate a corresponding test file in the backend/__tests__/services/ directory with the same name but with .test.ts extension. The test file should include basic unit tests for all exported functions from the service file. Use Jest testing framework with TypeScript. Include proper imports, describe blocks, and test cases that cover the main functionality of each exported function. Follow the project's TypeScript and testing conventions."
  }
}