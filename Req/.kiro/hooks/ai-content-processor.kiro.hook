{
  "enabled": true,
  "name": "AI Content Processor",
  "description": "Procesa automáticamente requests de IA usando el sistema de rotación gratuita",
  "version": "1.0",
  "when": {
    "type": "fileEdited",
    "patterns": [
      "**/tasks.md",
      "**/requirements.md"
    ]
  },
  "then": {
    "type": "askAgent",
    "prompt": "🤖 **AI Content Processor Activated**\n\nVoy a procesar automáticamente requests de IA usando el sistema de rotación gratuita.\n\n## 🔄 **Sistema de Rotación Automática:**\n\n### **1. Inicializar FreeAISystem**\n```javascript\nconst FreeAISystem = require('./free-ai-system.js');\nconst aiSystem = new FreeAISystem();\n\n// Configurar APIs gratuitas\nprocess.env.GROQ_API_KEY = 'gsk_...'; // Tu API key de Groq\nprocess.env.GOOGLE_API_KEY = 'AIza...'; // Tu API key de Gemini\nprocess.env.HUGGINGFACE_API_KEY = 'hf_...'; // Tu API key de HuggingFace\nprocess.env.COHERE_API_KEY = 'co_...'; // Tu API key de Cohere\n```\n\n### **2. Detectar Tareas de IA Pendientes**\n```bash\necho \"🔍 Buscando tareas de IA pendientes...\"\n\n# Buscar issues con label 'ai-content' abiertos\nAI_ISSUES=$(gh issue list --label \"ai-content\" --state open --json number,title,body)\n\nif [ -n \"$AI_ISSUES\" ]; then\n    echo \"🤖 Encontradas tareas de IA pendientes\"\n    echo \"$AI_ISSUES\" | jq -r '.[] | \"#\\(.number): \\(.title)\"'\n    \n    # Procesar cada issue de IA\n    echo \"$AI_ISSUES\" | jq -r '.[] | .number' | while read issue_num; do\n        echo \"📋 Procesando issue #$issue_num con IA...\"\n        process_ai_issue $issue_num\n    done\nelse\n    echo \"✅ No hay tareas de IA pendientes\"\nfi\n```\n\n### **3. Función para Procesar Issues de IA**\n```bash\nprocess_ai_issue() {\n    local issue_num=$1\n    \n    # Obtener detalles del issue\n    ISSUE_INFO=$(gh issue view $issue_num --json title,body,labels)\n    ISSUE_TITLE=$(echo \"$ISSUE_INFO\" | jq -r '.title')\n    ISSUE_BODY=$(echo \"$ISSUE_INFO\" | jq -r '.body')\n    \n    echo \"🤖 Procesando: $ISSUE_TITLE\"\n    \n    # Determinar tipo de tarea de IA\n    if echo \"$ISSUE_TITLE\" | grep -qi \"database\\|schema\"; then\n        TASK_TYPE=\"database_design\"\n    elif echo \"$ISSUE_TITLE\" | grep -qi \"frontend\\|component\"; then\n        TASK_TYPE=\"code_generation\"\n    elif echo \"$ISSUE_TITLE\" | grep -qi \"content\\|blog\\|article\"; then\n        TASK_TYPE=\"content_generation\"\n    else\n        TASK_TYPE=\"general_assistance\"\n    fi\n    \n    echo \"📋 Tipo de tarea: $TASK_TYPE\"\n    \n    # Crear prompt para IA\n    AI_PROMPT=\"Ayuda con esta tarea: $ISSUE_TITLE\\n\\nDetalles: $ISSUE_BODY\\n\\nTipo: $TASK_TYPE\"\n    \n    # Procesar con sistema de rotación\n    AI_RESULT=$(node -e \"\n        const FreeAISystem = require('./free-ai-system.js');\n        const aiSystem = new FreeAISystem();\n        \n        const request = {\n            type: '$TASK_TYPE',\n            prompt: '$AI_PROMPT',\n            issueId: $issue_num\n        };\n        \n        aiSystem.distributeRequest(request)\n            .then(result => {\n                console.log('✅ IA completada:', result.substring(0, 200));\n                return result;\n            })\n            .catch(error => {\n                console.log('❌ Error de IA:', error.message);\n            });\n    \")\n    \n    # Agregar resultado como comentario al issue\n    if [ -n \"$AI_RESULT\" ]; then\n        gh issue comment $issue_num --body \"🤖 **Resultado de IA Automática**\n        \n**Tipo de tarea:** $TASK_TYPE\n**Procesado con:** Sistema de rotación gratuita\n**Costo:** \\$0.00\n\n**Resultado:**\n$AI_RESULT\n\n*Generado automáticamente por AI Content Processor*\"\n        \n        echo \"✅ Resultado agregado al issue #$issue_num\"\n        \n        # Notificar a Telegram\n        send_telegram_notification $issue_num \"$TASK_TYPE\" \"completed\"\n    fi\n}\n```\n\n### **4. Notificación Automática**\n```bash\nsend_telegram_notification() {\n    local issue_num=$1\n    local task_type=$2\n    local status=$3\n    \n    local message=\"🤖 *IA Automática Completada*\n\n*Issue:* #$issue_num\n*Tipo:* $task_type\n*Estado:* $status\n*Costo:* \\$0.00\n\n*Sistema:* Rotación automática de APIs gratuitas\"\n    \n    curl -s -X POST \"https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/sendMessage\" \\\n        -H \"Content-Type: application/json\" \\\n        -d \"{\n            \\\"chat_id\\\": \\\"$TELEGRAM_CHAT_ID\\\",\n            \\\"text\\\": \\\"$message\\\",\n            \\\"parse_mode\\\": \\\"Markdown\\\"\n        }\" > /dev/null\n}\n```\n\n## 🔑 **APIs Reales - Necesitas las Keys:**\n\n### **Groq API (GRATIS - MUY RÁPIDO):**\n```bash\n# 1. Registrarse en: https://console.groq.com\n# 2. Ir a: https://console.groq.com/keys\n# 3. Crear API Key\n# 4. Configurar:\nexport GROQ_API_KEY=\"gsk_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx\"\n\n# Endpoint real:\nendpoint: 'https://api.groq.com/openai/v1/chat/completions'\n# Límites reales:\n# - 30 requests por minuto GRATIS\n# - 14,400 requests por día GRATIS\n```\n\n### **Google Gemini API (GRATIS):**\n```bash\n# 1. Ir a: https://makersuite.google.com/app/apikey\n# 2. Crear API Key\n# 3. Configurar:\nexport GOOGLE_API_KEY=\"AIzaxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx\"\n\n# Endpoint real:\nendpoint: 'https://generativelanguage.googleapis.com/v1beta/models/gemini-pro:generateContent'\n# Límites reales:\n# - 60 requests por minuto GRATIS\n# - 1,500 requests por día GRATIS\n```\n\n### **Hugging Face API (GRATIS):**\n```bash\n# 1. Registrarse en: https://huggingface.co/join\n# 2. Ir a: https://huggingface.co/settings/tokens\n# 3. Crear token\n# 4. Configurar:\nexport HUGGINGFACE_API_KEY=\"hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx\"\n\n# Endpoint real:\nendpoint: 'https://api-inference.huggingface.co/models/microsoft/DialoGPT-large'\n# Límites reales:\n# - 1,000 requests por hora GRATIS\n```\n\n## 📊 **¿Por qué esos límites específicos?**\n\n### **Límites Reales de APIs Gratuitas:**\n- **Groq:** 30 req/min, 14,400 req/día (documentado oficialmente)\n- **Gemini:** 60 req/min, 1,500 req/día (límite gratuito de Google)\n- **Hugging Face:** 1,000 req/hora (límite de inference API gratuita)\n- **Cohere:** 100,000 tokens/mes (plan gratuito)\n\n### **Capacidad Total Combinada:**\n```\nGroq: 14,400 req/día\nGemini: 1,500 req/día  \nHugging Face: 24,000 req/día (1000/hora × 24h)\nCohere: ~3,333 req/día (100k tokens ÷ 30 tokens promedio)\n\nTOTAL: ~43,233 requests/día GRATIS\n```\n\n## 🎯 **¿Necesita Hooks?**\n\n**SÍ, definitivamente necesita hooks para ser automático:**\n\n1. **Hook de activación** - Detecta cuando hay tareas de IA\n2. **Hook de procesamiento** - Ejecuta el FreeAISystem\n3. **Hook de notificación** - Informa resultados\n\n**¿Quieres que implemente el hook completo para que el sistema sea totalmente automático?**"
  }
}
